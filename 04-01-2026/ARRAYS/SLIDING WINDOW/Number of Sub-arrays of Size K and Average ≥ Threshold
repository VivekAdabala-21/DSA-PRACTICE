 int n = arr.length;
        int count = 0;
        int windowSum = 0;

        // First window
        for (int i = 0; i < k; i++) {
            windowSum += arr[i];
        }

        if (windowSum >= threshold * k) {
            count++;
        }

        // Slide the window
        for (int i = k; i < n; i++) {
            windowSum = windowSum + arr[i] - arr[i - k];
            if (windowSum >= threshold * k) {
                count++;
            }
        }

        return count;
